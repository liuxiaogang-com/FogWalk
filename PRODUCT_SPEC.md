# 迷雾足迹 iOS Demo：产品与技术规格

> 2026-09-04：V0.2 第一轮当前范围与最新验收记录以 `V02_PLAN.md` 为准。以下实现记录包含历史阶段，不代表后台记录、闭环或完整导航已完成。

更新时间：2026-09-03  
状态：空库导入/导出与持久化版本已实现、通过测试，并已安装启动于用户 iPhone  
工作目录：`/Users/xiao/Desktop/citywalk`

## 1. 产品目标

制作一款原生 iOS 足迹记录与探索应用。它既能记录、导入和回顾历史 GPS 足迹，也把地图表现成“战争迷雾”：未到访区域被半透明暗雾覆盖，真实经过的区域逐渐显露。

与普通足迹 App 的关键区别是主动探索：用户提供时间、交通方式和目标类型，应用从尚未探索的区域中推荐可到达的目的地，或生成起终点相同、尽量少重复且优先经过未知道路的闭环路线。

## 2. 已确认的产品原则

1. 使用 Apple MapKit 作为地图底图。
2. 原生 SwiftUI/MapKit 实现，第一阶段面向 iPhone 竖屏。
3. 第一版无需账号，不上传用户足迹；位置历史与导入结果本地保存。
4. 第一阶段先完成可运行 Demo，不制作纯静态原型。
5. 先在本机模拟器运行和测试；只有用户后续明确提出时才连接手机进行真机调试。
6. 开发阶段暂用名称“迷雾足迹”。

## 3. 演示数据

### 3.1 文件

- `demodata/backUpPhotoData.csv`
  - 3,519 个照片位置点。
  - 时间范围：2020-06-14 至 2026-05-30（Asia/Shanghai）。
  - 与轨迹 CSV 使用相同字段结构，但所有 `locType=0`、`accuracy=0`。
  - 不包含照片文件名或照片内容。
- `demodata/backUpData-all.csv`
  - 150,664 个连续定位点。
  - 时间范围：2023-09-05 至 2026-09-03（Asia/Shanghai）。
  - 包含时间、经纬度、方向、精度、速度、点间距离、前后台状态、海拔等字段。
- `demodata/backUpData-all.gpx`
  - 150,664 个 GPX 轨迹点。
  - 与完整 CSV 为同一批轨迹的通用交换格式。
- `demodata/IMG_8741.PNG`、`IMG_8742.PNG`、`IMG_8743.PNG`
  - “一生足迹”参考界面截图，仅参考地图主体、日期筛选、轨迹和统计信息层级，不照搬其品牌、图标或密集悬浮按钮布局。

### 3.2 导入与去重

三个数据文件都必须能够被导入器解析，以验证 CSV 和 GPX 兼容性。完整 CSV 和 GPX 的重复点使用“时间戳 + 归一化经纬度”去重，不能重复计入距离、轨迹点或探索区域。照片位置作为低置信度历史足迹来源。

原始数据保留；探索区域、日期分组、简化轨迹和统计结果属于可重新计算的派生数据。

## 4. 地图迷雾规则

### 4.1 基础视觉

- 轨迹中心 0–50 米：完全清晰，并计入已探索区域。
- 50–100 米：逐渐过渡到完整迷雾，表现“目之所及”。
- 100 米以外：保持深灰黑色半透明迷雾。
- 第一版使用柔和暗雾，不实现持续飘动的动画云，以保证地图可读性、性能与电量。

50 米暂时不作为普通用户设置项，但在代码中必须是可调整参数。后续如有需要，可增加“精确 / 标准 / 宽广”三档。

### 4.2 有效点与轨迹连接

每个有效定位点可以独立揭开周围 50 米。只有相邻点满足下列条件时，才连接成可信连续轨迹并揭开两点之间的区域：

1. 时间间隔不超过 5 分钟；
2. 直线距离不超过 300 米；
3. 定位精度不超过 100 米；
4. 推算速度没有明显异常；
5. 没有跨越日期或明显定位跳变。

距离 300 米至 1 千米的相邻点不直接用直线揭雾。后续具备路网匹配时，只有成功匹配到可信道路路线才连接。超过 1 千米或时间间隔过大的点直接断开。

“同一天”只用于日期聚合，并不意味着当天所有位置必须连成一条线。可信轨迹使用实线；不确定间隔可断开或用虚线表达，但不驱散中间迷雾；明显漂移点忽略。

### 4.3 来自真实数据的阈值依据

对完整 CSV 的 150,664 个点已做初步统计：

- 相邻点时间间隔中位数：16 秒；
- 92.3% 的相邻点间隔不超过 2 分钟；
- 相邻点距离中位数：约 93 米；
- 87.3% 的相邻点同时满足“5 分钟以内且 300 米以内”；
- 定位精度中位数：约 31 米；
- 约 95% 的点定位精度不超过 100 米。

## 5. 首页与信息架构

首页以地图为主体。底部采用简洁面板，包含：

- 当前筛选范围的轨迹点、距离与探索统计；
- “去探索”主按钮；
- 今日 / 七日 / 本月 / 一生筛选；
- 定位、图层、导入和设置入口。

地图允许缩放、平移和回到当前位置。模拟器默认定位到导入数据的最新位置；真机使用当前真实位置。提供“演示移动”能力，使模拟器可以沿一段轨迹移动并实时驱散迷雾。

## 6. 位置记录

应用支持前台和后台足迹记录。首次使用仅申请“使用 App 期间”定位；用户主动启用持续记录时，解释用途后再申请“始终允许”。

位置记录必须保留时间、经纬度、水平精度、速度、方向、海拔和来源。后台模式、电量策略、系统终止后的恢复和权限降级将在真机阶段验证；模拟器 Demo 先验证状态管理和数据管线。

## 7. 随机目的地

用户可选择：

- 时间：15 / 30 / 45 / 60 / 90 分钟；
- 方式：步行 / 骑行 / 驾车；
- 类型：任意 / 咖啡厅 / 公园 / 图书馆 / 餐饮 / 景点。

目的地模式中的时间表示从当前位置到目的地的单程预计时间。每次提供一个主推荐和一个备选，并支持“换一批”。

推荐不是均匀随机。候选地点根据下列因素打分，并在高分候选中保留一定随机性：

- 路线经过的未探索区域比例；
- 目的地是否位于迷雾区域；
- 是否符合时间预算；
- 是否存在适用于所选交通方式的可通行路线；
- 是否避免刚刚走过或反复推荐的路线与目的地；
- 能够取得时，考虑营业状态、可访问性和明显安全风险。

## 8. 闭环探索

闭环模式的时间表示从当前位置出发并回到原点的总活动时长。第一版优先支持步行与骑行，通过 2–3 个中间点形成环线，并以以下目标评分：

- 总预计时间接近预算；
- 去程和回程少重合；
- 路线中的未知区域比例更高；
- 避免明显不可达、私有、高速或危险道路。

允许约 ±15% 的时间误差，例如 30 分钟目标可接受约 25–35 分钟。

## 9. 地图、路网与导航技术分层

### 第一阶段：可运行 Demo

- Apple MapKit 显示地图、轨迹和迷雾；
- MapKit Local Search 搜索兴趣点；
- MapKit Directions 计算步行、骑行、驾车路线与预计时间；
- App 内展示推荐理由、路线、预计时间和未知比例；
- 点击“开始探索”后交给 Apple 地图正式导航；
- 闭环路线先通过候选中间点与多段路线完成可演示版本。

### 第二阶段：完整探索引擎

Apple 公开 MapKit API 不提供整座城市原始道路图的直接导出能力。因此后续使用 OpenStreetMap 路网数据构建指定城市范围内的本地道路图，用于：

- GPS 轨迹道路匹配；
- 已探索道路段统计；
- 未探索道路筛选；
- 重合度、可达性与环线优化；
- 离线或低请求量的候选生成。

MapKit 继续负责底图显示、兴趣点补充、路线复核与交给 Apple 地图导航。OSM 数据许可、署名、中国大陆坐标对齐与数据完整性必须在正式产品阶段专项验证。

## 10. Demo 分阶段交付

### 阶段 A：地图与迷雾预览

- 建立可编译运行的 iOS 工程；
- 解析三种演示数据并去重；
- 日期聚合和可信轨迹切段；
- 地图轨迹显示；
- 50 米清晰、50–100 米渐变的迷雾效果；
- 今日 / 七日 / 本月 / 一生筛选；
- 基本统计和模拟器预览。

### 阶段 B：探索操作

- “去探索”参数面板；
- 随机目的地候选；
- MapKit 路线和 ETA；
- 主推荐、备选与换一批；
- 开始导航入口。

### 阶段 C：闭环与本地路网验证

- 闭环候选生成和重合度评分；
- 指定小范围 OSM 路网导入验证；
- 路段探索状态和探索率原型。

## 11. 第一阶段验收标准

1. 工程在本机 iOS 模拟器中可以编译、安装并启动。
2. 三个数据文件都能被解析，CSV 与 GPX 重复点不会重复计数。
3. 大体量数据导入不会阻塞界面到不可用。
4. 地图能显示真实演示数据所在区域，轨迹不会跨越明显时间或空间断点错误连线。
5. 迷雾能正确覆盖未探索区域，并沿可信轨迹形成 50 米清晰核心和 50–100 米渐变边缘。
6. 时间筛选会同步更新轨迹和统计。
7. Demo 在未获得用户新指示前不访问或连接用户真机。

## 12. 暂不承诺的事项

- 第一阶段不自行实现逐路口语音导航；
- 第一阶段不下载或处理全国完整路网；
- 模拟器测试不能替代后台定位、电量和系统权限的真机验证；
- 正式上架前仍需验证隐私声明、定位用途说明、OSM 许可与中国大陆地图数据合规问题。

## 13. 2026-09-03 第一版实现记录

### 已完成

- 已创建可运行的 `FogWalk.xcodeproj`，开发名称和界面名称为“迷雾足迹”；
- 三份个人数据仅作为本地测试输入，不再随 App 打包；完整解析、合并和去重得到 154,183 个唯一位置；
- 已实现可信轨迹切段、今日 / 七日 / 本月 / 一生筛选、距离和点数统计；
- 已实现 MapKit 底图、橙色轨迹，以及 0–50 米清晰、50–100 米渐变、外部暗雾的自定义覆盖层；
- 已实现“去探索”面板、目的地与闭环模式、时间 / 交通方式 / 地点类型参数、MapKit 兴趣点搜索和路线候选代码、打开 Apple 地图入口；
- 已加入前台定位与记录框架；用户明确授权后完成真机签名、安装和启动。

### 本地验证证据

- iPhone 17 Pro、iOS 26.5 模拟器完成编译、安装和启动；
- XCTest 共 8 项，全部通过、0 失败；覆盖完整数据解析去重、连接阈值、照片点隔离、时间筛选、闭环候选、重复导入和持久化重载；
- 首页与探索面板的验收截图保存在 `artifacts/`；
- 完整测试结果保存在 `.build/DerivedData/Logs/Test/`（该目录属于本地构建产物，不纳入源码交付）。

### 下一阶段边界

- 真正按道路生成低重合闭环，需要接入指定城市范围的 OSM 路网、地图匹配和图搜索；
- 当前持续记录仅为前台框架，后台定位、电量、系统终止恢复和权限体验必须在真机专项验证；
- 当前已改为一次导入后直接读取本地档案；数据规模继续增长时再迁移为分块数据库与增量索引。

## 14. 2026-09-03 空库与数据管理改造

- App 首次安装为空库，不携带用户的 CSV、GPX 或生成后的足迹档案；
- 首页中央和右上角数据菜单都提供导入入口；支持多选 CSV、GPX，以及导入 `.fogwalk` 备份；
- 导入时与现有本地数据合并并去重，重复导入同一文件不会增加唯一位置数；
- 导入结果以二进制 `.fogwalk` 档案原子写入 App 的 Application Support，后续冷启动直接读取；
- 右上角数据菜单提供导出入口，生成一个可再次导入的 `.fogwalk` 文件；
- 模拟器冷启动已经验证空库界面，App 安装包内确认没有 CSV、GPX 或 `.fogwalk`；
- XCTest 增至 8 项并全部通过，新增覆盖重复导入、持久化保存和重新加载；
- 真机包已使用 Apple Development 身份成功签名，bundle ID 为 `com.citywalk.FogWalkDemo`。
- 已安装到用户的 iPhone 16 Pro（iOS 26.6.1）并成功启动；进程与已安装 App 列表均完成核验。

## 15. 2026-09-03 地图与目的地修正

- 导入的 GPS/GPX 坐标继续以原始 WGS-84 保存和导出；仅在中国大陆 Apple 地图显示、探索网格和路线请求边界转换为地图坐标，避免约数百米的道路偏移；
- 目的地搜索增加中文自然语言查询兜底，“任意”会从公园、咖啡厅、图书馆、博物馆和景点中选取有名称的明确终点；
- MapKit 无法返回路线时不再把错误 5 直接展示给用户；仍可提供明确终点，但会标记为“终点候选”和“约”时间；
- MapKit 成功返回路线时，使用真实步行 / 骑行 / 驾车距离和 ETA，并明确标记“路线已验证”；
- 本机真实服务探测在校准后的郑州坐标搜索到 25 个公园，抽查前 8 个均成功返回步行路线；
- 暂时隐藏闭环模式，不再返回仅有最远点、实际不能成环的假推荐；
- 切换时间、交通方式或地点类型时立即清除旧推荐，避免参数和卡片不一致；
- 迷雾改为深色 Apple 地图、18 层连续羽化和橙色微光轨迹，减弱原来的白色硬边“灯管”效果；
- XCTest 共 9 项、0 失败，新增中国大陆坐标校准与禁止假闭环测试。

## 16. 2026-09-03 地图首页与未知探索重构

- 首页移除“迷雾足迹 / 位置数量”大标题卡，地图成为默认视觉主体；轨迹点、可信距离和 50 米半径收为左上角小胶囊；记录与导入导出分开放在右上角；
- 迷雾、轨迹和定位按钮改为带文字的小胶囊，记录不再混在四个无文字图标中；“去探索”保持底部主按钮，日期筛选移到它下方并降低视觉权重；
- “去探索”由半屏抽屉改为完整二级地图页。参数面板可收起，选中推荐后仍可看到本 App 的历史迷雾、青色推荐路线和橙色终点；只有“开始导航”才跳转 Apple 地图；
- 探索地图始终使用“一生”足迹判断已探索范围，不受首页“今日 / 七日 / 本月”查看筛选影响；
- 探索网格使用 25 米栅格表达 50 米探索半径；只有符合可信连接规则的相邻 GPS 点才会插值填充中间路段，照片点和断裂轨迹不会被错误连成已探索区域；
- 推荐地点首先硬性排除 50 米探索范围内的终点，并要求终点周边有足够未知区域；路线成功后，再按沿途未知比例 55%、终点周边未知比例 30%、时间匹配度 15% 排序；
- 卡片同时展示预计时间、距离、沿途未知比例和终点周边未知比例；路线不可用时只显示明确终点，不用直线冒充可导航路线；
- 探索页顶部另设“选点”模式，不挤占随机推荐卡。进入后长按地图约半秒落下终点旗标，反向解析地点和地址，并显示直线距离及“已探索 / 未探索”状态；确认后才交给 Apple 地图导航；
- 目的地条件区改为一行三个下拉项，不再同时铺开所有时间、方式和类型；推荐结果改为单层摘要面板，去掉卡片嵌套与横向大卡，仅保留名称、时间、距离、未知比例、备选切换、条件和导航；
- 进入探索页时以当前定位为中心显示约 3 公里的本地范围；GPS 尚未返回时先使用最近一条导入位置，定位更新后只自动校准一次，避免打开全国范围和反复抢夺用户手势；
- 默认只开启迷雾，历史轨迹线默认关闭；推荐生成后的路线仍单独显示，不受历史轨迹开关影响；
- 首页“定位”按钮会重新请求实时位置，并把当前位置恢复到固定约 3 公里缩放，而不是只刷新定位数据；
- 咖啡厅检索补充瑞幸、库迪、星巴克、蜜雪冰城等清晰品牌关键词，餐饮检索补充饭店、快餐和小吃；结构化 POI 与中文关键词结果始终合并，明确品牌和餐饮名称获得排序加权；
- 推荐继续硬性排除已探索终点，但当 50 米外的候选周边仅部分被经过时采用分级放宽，避免餐饮候选全部被过度过滤；
- 探索结果采用“地点地图”交互：最多展示 6 个候选，全部以带序号的标记显示在地图上；上方固定时间、方式和类型条件，下方使用约 80% 屏宽的横向卡片并露出下一张；点击地图标记或左右滑动卡片都会同步选中地点并切换路线；
- XCTest 共 12 项、0 失败，包含 50 米探索半径、可信轨迹段插值，以及明确咖啡与餐饮名称排序测试；主地图与完整探索页已在 iPhone 17 Pro 模拟器完成视觉检查。
