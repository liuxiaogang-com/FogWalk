# V0.3 双模式出行记录

> 2026-09-04 V0.3.1 更新：用户澄清运动权限可以保留，但需讲清省电依据。
> 现改为“运动辅助省电”可选开关，默认关闭，开启并开始记录才请求权限；关闭/拒绝仍可完整记录。
> 本文下方 V0.3 为历史验证，当前功能清单和本轮验证见 FUNCTIONAL_AUDIT.md。
> CMMotionActivityManager 为系统活动变化事件，不是持续原始加速度采样。潜在收益来自减少 GPS 工作，不承诺比纯定位更省电，必须对照测量。

## 用户需求与既有状态（2026-09-04）

用户询问定位是否成功，并要求正常/省电模式，支持前台、后台、锁屏，运动自适应以控制耗电。
V0.2.1 仅有单次定位/前台测试，不具备轨迹落盘和后台能力；此前缓存耗时测试不能证明定位或耗电表现。

## 本轮实现

- 显式“开始记录/结束记录”，默认关闭，模式持久化；记录面板展示权限、最新定位时间/精度、已保存点数与错误。
- 正常模式：运动时 Best 精度、15m distanceFilter；有效点至少间隔 5s 且移动约 10m（结合精度抑制漂移）。
- 省电模式：运动时 HundredMeters 精度、60m distanceFilter；有效点至少间隔 20s 且移动约 40m。以降低轨迹细节换取较低定位需求。
- 上述秒数为应用接纳点的下限，不是 iOS 保证的定位定时器。不能把减少点数当作已测得的耗电下降。
- Core Motion 活动分类结合位置速度/位移；静止持续正常 120s、省电 90s 后降级，移动立即恢复；不启用持续原始加速度采样。
- Always 授权且可用时，以区域退出/显著位置变化辅助恢复；省电静止可停止标准定位。没有 Always 时保留低精度标准定位且不自动暂停，避免后台无法恢复。
- 后台模式、可见的定位指示、状态恢复；锁屏写入使用首次解锁后可用的数据保护级别。
- SQLite WAL 增量落盘，不在后台每个点重算 15 万点。启动保留原缓存，叠加增量；导入/导出合并时去重并用本地日志检查点防止重复。
- 前台限频刷新迷雾；后台只保存。保存失败明确报错，不显示成功。

## 系统边界

- 用户必须授予定位权限；建议“始终”及“精确位置”。运动权限拒绝时回退位置速度/位移判断。
- Core Motion 回调不能独立保证唤醒挂起 App。区域/显著位置变化也非即时，省电模式静止后刚起步的短段可能遗漏。
- 用户划掉 App、关闭定位、撤销权限、重启未首次解锁等情况不能保证连续记录。重新打开时根据之前的记录意图恢复并明确显示状态。
- 不声称后台定时器每 N 秒可靠执行，不使用虚假音频保活。
- 待外出 A/B：相同路线、时长、屏幕/网络条件，比较记录完整度和系统电池统计。预计省电不是实测省电百分比。

## 验收清单

- [x] 运动/静止状态、时间/距离/精度过滤与跳点测试。
- [x] SQLite 重启恢复、原档案兼容、增量导入导出、检查点及缓存保持测试。
- [x] 本地构建与回归；模拟位置回调验证持久化；真机定位和保存已取得日志证据。
- [x] V0.3 覆盖安装；明确外出、后台、锁屏和耗电仍需实测。

## 实现细节与验证

- 原档案继续支持 v1，新导出使用 v2（新增设备采集来源与可选的本地日志检查点）。外部导入不会套用另一台手机的日志检查点。
- SQLite 记录位于 Application Support/FogWalk/Recording，WAL + FULL 同步事务；原档案、用户数据未清空。失败批次保留在内存并提示重试，未落盘不计为成功。
- 热路径只追加 SQLite；前台约 15 秒合并刷新一次迷雾；后台不触发地图重建。恢复时原缓存 + 未归档的增量记录，保留快速启动。
- 当前定位只接受约 30 秒内的更新；记录会接收最多 5 分钟的延迟交付样本，仍按时间排序和可信条件过滤；较旧记录不会作为实时坐标。
- 正常模式拒绝精度差于 65m 的轨迹点，省电拒绝差于 100m 的点。省电的系统期望精度为 100m，不保证每次能满足，因此点数可能明显减少。
- 真机授权日志：status=4（When In Use），accuracy=0（Full Accuracy）。用户仍需申请 Always 才能启用区域/显著变化辅助恢复。
- 真机 2026-09-04 07:57:08：`FOGWALK_FIX accuracy=20.3 age=10.0 recording=0`。
- 真机 07:57:17：`FOGWALK_FIX accuracy=17.8 age=2.2 recording=1`，随后 `FOGWALK_RECORD saved=1 context=foreground`。
- 上述确认真机定位及前台持久化链路，不代表锁屏外出、后台唤醒或耗电已经通过。
- 最终全量 36 项通过，0 失败、0 跳过。结果：`/Users/xiao/Library/Developer/Xcode/DerivedData/FogWalk-gheyousbgdfsehgmwultxvdoufrs/Logs/Test/Test-FogWalk-2026.09.04_07-57-51-+0800.xcresult`。
- Release 0.3 (4) 构建、代码签名校验通过；已覆盖安装，未卸载，原 150,664 点档案保留。设备新增位置只写独立日志。
- 收尾再次确认手机安装版本 0.3 / 4，并用正常启动命令打开记录面板；模拟器页面截图 `artifacts/v03-recording-panel.png` 已检查（合成地图参考位置，非手机截屏）。
- 模拟器授权仅用于回调测试：CLLocation delegate 的前台/后台上下文共 2 个模拟点真实进入 SQLite 并重新读取；该测试不模拟操作系统真实挂起、锁屏或运动传感器精度。
- 曾遇 Swift 编译器对 SwiftUI Binding 的方法引用崩溃，改为显式闭包后构建通过。

## 外出验收步骤（需要用户参与）

1. 在“记录”面板选择正常，授予定位及运动权限，建议“始终”和“精确位置”，确认最近定位时间/精度更新。
2. 点开始记录，在前台走 2 分钟，再切其他 App 走 5 分钟，再锁屏走 10 分钟；不划掉 App。
3. 回到 App，确认保存点数增加、已走区域出现，结束记录；重新打开并导出备份验证记录仍在。
4. 用省电模式走相同路线和相近时长，保持屏幕、网络和其他 App 条件尽量一致；比较轨迹缺口与系统电池统计。
5. 静止 3 分钟后重新走动，分别测试正常/省电的恢复时延。没有 Always 时不要期待自动唤醒能力。

代码和模拟回调测试不替代上述测试；不宣称已节省某个百分比电量。

尚未实现按日自动归档/压缩长期增量日志；当前原档案走快速缓存，新记录只叠加日志。累计大量新记录后，应进一步测量启动合并成本，并做安全归档。

## 官方依据

- https://developer.apple.com/documentation/corelocation/handling-location-updates-in-the-background
- https://developer.apple.com/documentation/corelocation/cllocationmanager/pauseslocationupdatesautomatically
- https://developer.apple.com/documentation/coremotion/cmmotionactivitymanager
- https://developer.apple.com/library/archive/documentation/Performance/Conceptual/EnergyGuide-iOS/LocationBestPractices.html

后续接续以本文件为准，不要将旧版“前台定位测试”误认为完整记录。
