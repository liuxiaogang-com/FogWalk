# V0.3.1 功能核对与权限说明

日期：2026-09-04。当前版本 0.3.1 / 5。本文是后续接续的当前依据；历史 PRODUCT_SPEC、V02_PLAN、RECORDING_PLAN 不等于当前功能承诺。

## 用户最新决定

- 所有可见入口应有真实作用，不提供 Demo、假结果或“开发中”按钮。
- 运动权限不必强行删除：若有实际价值可以保留，但不能以未经测试的“更省电”强迫授权。
- 保留正常/省电模式，以及前台、后台、锁屏的记录设计；真实耗电和系统挂起行为必须另外验收。

## 运动权限的结论与实现

- 位置记录不依赖运动权限。新增“运动辅助省电”开关，默认关闭，独立持久化。
- 只有用户开启开关且开始记录，才调用 CMMotionActivityManager。关闭立即停止活动订阅和相关前台计时，不阻止位置记录。拒绝权限时回退位置速度/位移。
- 活动分类能补充静止/移动信息，从而有机会减少 GPS 高精度工作。但 Core Location 本身也能节能；实际差异取决于行程、环境、唤醒策略。当前没有开启比关闭更省电的 A/B 证据。
- 不采集原始加速度。仅凭 GPS 判断的移动显示“移动中”，不再误标“步行”；静止状态显示修正。
- 对照方案：固定同一种记录模式，相同路线、时长、屏幕、网络及其他 App 状态，分别关闭/开启运动辅助，多次重复；比较轨迹缺口、起步恢复延迟及系统能耗记录。不能用少存点数代替电量证据。

## 逐入口核对

| 页面/入口 | 实际行为与本轮处理 |
| --- | --- |
| 首页地图 | 地图优先，默认迷雾开/轨迹关；定位回到固定约 3km 范围，过期位置不再冒充实时位置 |
| 足迹回顾 | 今日/七日/本月改用真实当前日期，而非最后一条历史记录日期；跨日更新日期视图；旧档案不在当前范围时直接清空日期层，保留累计迷雾缓存；一生仍展示全部历史 |
| 图层 | 真实控制迷雾/历史轨迹；与日期筛选的作用分离；图层与回顾面板支持扩展高度 |
| 去探索 | 时间、方式、类型条件；硬排除已探索终点；真实道路与 ETA 预算；滑卡/点标记同步选中线路 |
| 无路线/网络失败 | 明确提示并可重试；取消自动生成无道路、直线估算推荐，不伪造成功 |
| 换一批/切换条件 | 请求取消、代次校验、去重；新增足迹使推荐失效后同步选中项 |
| 地图选点 | 独立长按模式；反查仅提供地址，不再把终点偷偷挪到地址中心；预览真实路线；退出按钮文案修正 |
| 目的地详情 | 在本 App 看路线/迷雾；明确按钮跳 Apple 地图提供转向指引；打开失败提示；删除“未持续保存”的过期说明 |
| 出行记录 | 真正保存 SQLite；正常/省电；可选运动辅助；点数跨进程保留；位置错误不再显示无效“重试保存”按钮 |
| 导入 | CSV / 照片 CSV / GPX / .fogwalk，文件选择、解析、去重、原档案保护、缓存与增量记录合并 |
| 导出 | .fogwalk 可回导；读取前等待已接纳位置的写入；保存失败则导出明确失败，不遗漏待保存批次而报告成功 |
| 空库/异常 | 空库可定位探索；数据恢复期间不把未知的探索状态当未探索；不自动覆盖损坏原档案 |

## 明确不属于当前成品范围

- 全路网解析、真正低重合闭环路线尚不存在。用户此前允许先做好目的地；本版删除死代码中的闭环占位，不把去最远点当环线。没有对应 UI 入口。
- App 内逐转向语音导航未实现；当前提供的是 App 内真实路线预览 + 显式 Apple 地图导航，不用改文案冒充已有自研导航。
- 无个人数据内置。DEBUG 模拟器的合成 UI 测试数据仍用于回归，不会进入 Release 真机。
- `com.citywalk.FogWalkDemo` 仅为既有内部 Bundle ID，保留以延续安装和用户数据，不代表用户界面是 Demo。
- 长期新增日志尚无自动归档；这不是可见半成品入口，但累积大量新记录后的启动成本仍需长期测量。

## 验证记录

- 第一轮发现旧测试把历史数据的最后一天当“今日”，修正为显式测试历史日期，并另加真正“今日/本月不含旧数据”的用例。
- 回归 39 项通过，0 失败、0 跳过；覆盖三份真实导入数据、档案回导、SQLite 回调持久化、立即保存屏障、运动辅助默认关闭及记忆、GPS 静止恢复、无路线不输出估算推荐、日期筛选、地图标记与镜头交互。
- 首轮完整通过结果：`artifacts/v031-final.xcresult`。日期缓存跨日优化后的最终结果：`artifacts/v031-verified.xcresult`，同样 39 项通过、0 失败、0 跳过。
- Release 0.3.1 / 5 构建、签名检查通过；包内无 CSV/GPX/.fogwalk 个人数据，二进制未发现 UIFixture 和 fixture 启动入口。
- 手机 08:08 覆盖安装成功、正常启动成功，08:09 查询安装版本为 0.3.1 / 5。跨日缓存优化的最终包于 08:11 再次覆盖安装并正常启动成功。未卸载、未清空原档案或新增记录数据库。
- 检查模拟器截图 `artifacts/v031-recording.png`、`artifacts/v031-detail.png`；后者仅为 Debug 合成地点的布局验证，不是真实地点服务成功证据。
- 本轮 Debug 模拟器 154,183 点专项基准：旧全量路径约 5.52s，首次生成新缓存约 4.79s，读取缓存约 0.087–0.089s。这不是手机秒开计时，也不把测试主线程阻塞的启动日志当生产性能结论。
- 既有 V0.3 真机日志只证明前台定位与落盘；本轮自动回调测试不能证明系统真实挂起、锁屏外出或耗电通过。当前网络下每种 POI/交通方式的服务可用性、Apple 地图跳转实操仍需真机外出验收；无服务时应明确报错。

## 官方依据

- [Apple：高效获取位置](https://developer.apple.com/documentation/xcode/accessing-the-device-s-location-efficiently)：按需要降低位置更新；系统定位也有静止检测。
- [Apple：CMMotionActivityManager](https://developer.apple.com/documentation/coremotion/cmmotionactivitymanager)：活动变化事件及运动隐私授权。
- [Apple：自动暂停定位](https://developer.apple.com/documentation/corelocation/cllocationmanager/pauseslocationupdatesautomatically)：自动暂停及恢复边界。
