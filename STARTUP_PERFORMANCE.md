# V0.2.1 大数据启动优化

2026-09-04 用户要求覆盖安装手机，并优化每次打开的长时间转圈。

## 已确认原因与方案

- 旧启动链路：完整二进制 plist 档案解码 → 全量 ExplorationGrid → 全量轨迹与日期轨迹 → 才创建地图。样本共有 154,183 个唯一位置。
- 改为地图先创建、后台恢复；保存派生的探索网格和四种日期轨迹快照，后续启动无需解码原始档案或重算网格。
- 缓存包含版本、时区/日历与原档案文件指纹；过期、缺失或损坏时回退重建。原始档案格式不变、绝不以缓存替换原始数据。
- 坐标与网格使用紧凑无损二进制块，避免每个数值一个 plist 对象。导入后刷新缓存，导出仍使用完整原始数据。
- 缓存恢复完成前不允许自动推荐，避免把尚未读完的足迹误判为未探索。导入也需等原始数据读完再合并，防止覆盖旧数据。
- 首次升级需要一次缓存构建；“秒开”为优化目标，不承诺网络地图瓦片或首次定位也能一秒完成。

## 验收与接续

- [x] 缓存往返、失效、损坏回退、懒加载导出/合并保护测试。
- [x] 使用相同 154,183 点资料比较旧启动准备与缓存恢复耗时（不输出坐标）。
- [x] 构建及回归；模拟器首页截图检查完成（`artifacts/v021-home.png`），地图和操作布局正常。
- [x] 已在手机覆盖安装并启动，不卸载、不清空数据。

设备首次安装尝试：CoreDeviceError 1011，iPhone unavailable；已请用户解锁/信任/重新连接。

## 实测结果

### 模拟器：相同数据与编译模式的准备阶段对比

iPhone 17 Pro / iOS 26.5 模拟器，Debug；154,183 个唯一点：

- 旧链路（解码原档案 + 网格 + 一生/今日轨迹）：5.544 秒。
- 首次构建并写入缓存（原档案已在内存）：4.780 秒。首次升级仍需原档案解码，不能把该项当作完整首次启动时间。
- 缓存读取、SHA-256 校验、网格与四种轨迹还原，三次：0.097 / 0.088 / 0.088 秒，中位数相对旧链路约 63 倍。
- 缓存 8,470,481 字节，约 8.47 MB；原始档案格式及内容不改变。
- 上述是数据准备阶段，不包含进程创建、地图图层实际绘制、网络瓦片、首次 GPS 定位，也不是清空系统磁盘缓存后的冷盘测试。

### 真机

- iPhone 16 Pro；先覆盖安装 V0.2，再覆盖安装 Release 优化构建 V0.2.1 (3)。Bundle ID 保持 `com.citywalk.FogWalkDemo`。
- 两次进程重新启动日志：`FOGWALK_STARTUP path=cache points=150664 seconds=0.057`、`seconds=0.063`。
- 手机上的 150,664 点是现有档案的统计，与本地含照片数据的 154,183 点测试集不同。本次没有向手机导入或替换个人档案。
- 首次迁移已经在手机生成缓存，但未捕获该次耗时；不能将上述缓存恢复数字当作首次迁移或整机首屏耗时。
- 控制台采集使用有时限的 `devicectl --console`，命令到时退出不代表 App 崩溃。
- 已再次通过正常启动命令打开手机 App；设备应用清单确认安装版本为 0.2.1、构建号 3。

### 测试证据

- 27 项全量测试通过，0 失败，0 跳过：`Test-FogWalk-2026.09.04_07-38-35-+0800.xcresult`。
- 最后补充异常处理与计时日志后，启动专项 7 项再次通过：`Test-FogWalk-2026.09.04_07-39-42-+0800.xcresult`。
- xcresult 位于 `/Users/xiao/Library/Developer/Xcode/DerivedData/FogWalk-gheyousbgdfsehgmwultxvdoufrs/Logs/Test/`。
- 基准输出保存在本地忽略目录 `artifacts/startup-test-diagnostics/`，不提交个人日志。
- Release 构建成功，`codesign --verify --deep --strict` 通过。App 路径：`.build/DeviceDerivedData/Build/Products/Release-iphoneos/FogWalk.app`。

## 维护要点

- 缓存命中依赖原档案 inode/大小/修改时间，原档案原子替换会失效。校验和防止损坏缓存被用于探索判断。
- 修改坐标校准、连线规则、探索半径或缓存结构时，必须增加 `StartupSnapshot.schemaVersion`。
- 时区或日历变化会失效；缓存写入失败只影响下次性能，不影响原始数据，可重建。
- 原有持续后台记录、闭环路网等未实施范围不变。
