# Citywalk / 迷雾足迹：新会话接续说明

更新时间：2026-09-13（北京时间）。本文用于在 Windows 上新开 Codex 项目和会话，继续修改功能。版本、提交和构建记录是本次交接的快照，后续开发先核对实际 Git 与 GitHub 状态。

## 1. 新项目应该打开哪里

在 Codex 添加下面这个目录作为项目，然后在该项目内新开会话：

```text
D:\02_Dev\最终迁移包-2026-09-04\02-Citywalk-已提交Git-完整项目\Citywalk\project
```

这里包含 `.git`、`FogWalk.xcodeproj` 和应用源码。此前的会话挂在 `D:\02_Dev\tools\codex-plus`，但 Citywalk 的实际修改和 Git 操作都在上面的独立仓库完成。

外层 `Citywalk` 是迁移包目录，包含聊天记录、迁移资料、原始会话和本次下载的构建产物；Git 仓库在其 `project` 子目录。沿用已有仓库和远程即可。

给新会话的开场文字可直接复制：

> 请先读取当前项目的《新会话接续说明.md》，再按需读取 README.md、PRODUCT_SPEC.md、FUNCTIONAL_AUDIT.md、WINDOWS_INSTALL.md 和 SIMULATED_LOCATION_TEST.md。确认当前目录、Git 状态、远程仓库及最新 Release。这个项目在 Windows 修改原生 iOS Swift 代码，通过 GitHub Actions 的 macOS/Xcode 构建，成功后自动发布未签名 IPA 到私有 Releases。请保留个人数据、现有提交历史和自动发布流程。接下来我会给出具体的功能修改需求。

本轮只完成了云端构建、发布和交接准备；下一项功能修改尚未指定。

## 2. 当前项目是什么

迷雾足迹（FogWalk）是原生 iPhone App，使用 SwiftUI、MapKit、Core Location 和 SQLite。它最初在 Mac 上开发，目前保持原生 iOS 工程。

主要功能：导入照片位置 CSV、轨迹 CSV、GPX 和 `.fogwalk` 备份；地图迷雾与历史轨迹；正常/省电记录模式；后台定位与增量保存；探索目的地、路线预览、高德/Apple 地图导航选择。

当前构建参数：

| 项目 | 值 |
| --- | --- |
| 工程 / Scheme | FogWalk.xcodeproj / FogWalk |
| App 版本 | 0.3.2 |
| 最低系统 | iOS 26.0，iPhone |
| Bundle ID | com.citywalk.FogWalkDemo |
| 云端环境 | macos-26 / Xcode 26.6 |
| 发布产物 | Release 配置、arm64、iPhoneOS、未签名 IPA |

Windows 可编辑源码、维护 Git、运行适用的静态检查、触发云端构建和下载产物。iOS 编译、XCTest 和模拟器验证由 macOS/Xcode 执行；Windows 上的编辑或语法检查不能代替它们。

## 3. Git、Releases 和已完成验证

- 私有仓库：[liuxiaogang-com/Citywalk](https://github.com/liuxiaogang-com/Citywalk)
- 远程：`origin = https://github.com/liuxiaogang-com/Citywalk.git`
- 主分支：`main`，原有历史和 `v0.1-baseline` 标签已保留。
- 已验证的构建源码提交：`37bde2b1a17be85ec33235d858774dca7a9c5c60`。本交接文档随后单独提交，文档提交不等于新的 App 构建。
- 最新已验证 Release：[v0.3.2-build1003](https://github.com/liuxiaogang-com/Citywalk/releases/tag/v0.3.2-build1003)
- 上一版也已保存：[v0.3.2-build1002](https://github.com/liuxiaogang-com/Citywalk/releases/tag/v0.3.2-build1002)
- 历史版本入口：[Releases](https://github.com/liuxiaogang-com/Citywalk/releases)
- 完整成功运行：[Actions 34704353870](https://github.com/liuxiaogang-com/Citywalk/actions/runs/34704353870)

最后这轮云端验证：39 项测试通过、1 项因模拟器定位权限未授权而按原测试逻辑跳过、0 失败。另有 3 项依赖未入库个人轨迹数据的测试在 CI 配置中预先排除。不能描述为“43 项全部通过”。

build 1003 已从 Release 下载，并验证 SHA-256、ZIP CRC、arm64、iPhoneOS、最低系统版本、未签名状态、可执行权限和后台定位配置。文件名为：

```text
FogWalk-0.3.2-build1003-37bde2b1-unsigned.ipa
```

本地目录（在 Git 仓库外）：

```text
D:\02_Dev\最终迁移包-2026-09-04\02-Citywalk-已提交Git-完整项目\Citywalk\构建产物\34704353870
```

其中 `build-info.json` 记录源码提交、版本、构建环境和哈希；`local-verification.json` 记录本地验证。此轮没有从 Windows 安装到实际手机。旧文档中“已安装 iPhone 16 Pro”的表述属于此前 Mac 开发阶段。

## 4. Windows 修改功能后的提交与构建

1. 在正确仓库读取 `git status --short --branch`、`git diff` 和相关需求文档，保留已有未提交改动。
2. 修改对应 Swift 文件；涉及行为变化时更新有意义的现有 XCTest。业务规则以 [PRODUCT_SPEC.md](PRODUCT_SPEC.md) 和 [FUNCTIONAL_AUDIT.md](FUNCTIONAL_AUDIT.md) 为依据。
3. Windows 先做适用的静态检查与 `git diff --check`。涉及 CI 脚本时检查 Bash/Python/工作流语法。SwiftUI、MapKit 等的真正编译结果以云端 Xcode 为准。
4. 用 `git add -- <本次实际修改的文件>` 明确暂存范围，检查 `git diff --cached`，然后提交并推送。当前可在 `main` 继续；若使用 `codex/<功能名>` 分支，其变更合并并推送到 `main` 后才触发当前自动构建。
5. 推送符合触发条件的内容后，跟进 Actions 直到成功或取得失败日志。构建成功后检查对应 Release 的四个附件及源码提交；修改涉及真机行为时再做安装验收。

工作流文件：[.github/workflows/ios-unsigned.yml](.github/workflows/ios-unsigned.yml)。

自动触发条件是推送到 `main`，且变更匹配以下范围：`FogWalk/**`、`FogWalkTests/**`、`FogWalk.xcodeproj/**`、`FogWalk-Info.plist`、`scripts/ci-*.sh`、`scripts/ci-*.py` 或工作流自身。普通说明文档提交不会自动构建，也不会发布新 Release。还可以手动运行 `workflow_dispatch`。

发布链路：

```text
Windows 修改 → git commit / push main
→ GitHub macOS 测试 → 未签名真机归档 → IPA 校验
→ 上传 Actions artifact → 发布 GitHub Release
→ Windows 下载 → 自己的 Apple ID 重新签名 → iPhone 验收
```

相关脚本：

- [scripts/ci-test.sh](scripts/ci-test.sh)：选择可用 iOS 26+ 模拟器，运行测试并排除 3 项个人数据测试。
- [scripts/ci-build-unsigned.sh](scripts/ci-build-unsigned.sh)：归档、打 IPA、生成 SHA-256 和构建信息。
- [scripts/ci-publish-release.py](scripts/ci-publish-release.py)：校验附件，以草稿上传全部文件后发布；拒绝覆盖已有 Release。

App 构建号自动使用 `1000 + GITHUB_RUN_NUMBER`，无需为每次 CI 手工修改本地的 build 6。产品版本需要变化时，统一修改工程内相关 `MARKETING_VERSION`。Release 标签形如 `v0.3.2-build1003`，重跑追加 `-r2` 等后缀；标签对应实际打包的源码提交。

Release 长期保存 IPA、SHA-256、build-info.json、WINDOWS_INSTALL.md，保留历史版本；Actions artifact 另存 30 天，诊断日志和 xcresult 保留 7 天。私有仓库下载需要有权限的 GitHub 账号。云端发布使用内置 `GITHUB_TOKEN` 和 `contents: write`，无需额外保存个人令牌或 Apple 签名资料。

## 5. 本机命令与工具位置

Windows 命令优先用 PowerShell 7（`pwsh.exe`），中文文件显式按 UTF-8 读取。当前会话中 `gh` 不在 PATH，但可用的已登录 GH CLI 位于另一个迁移包的工具目录：

```powershell
$gh = 'D:\02_Dev\最终迁移包-2026-09-04\01-AudioLife-已提交Git-完整项目\AudioLife\.tools\github-cli\bin\gh.exe'
& $gh auth status
& $gh run list --repo liuxiaogang-com/Citywalk --limit 5
& $gh release list --repo liuxiaogang-com/Citywalk --limit 10
```

这里只复用工具可执行文件，Citywalk 的代码仍在自己的仓库。若搬移或删除迁移包后工具路径失效，重新查找 GH CLI 或正常安装它，不应因此重新初始化项目。不要输出或写入令牌、Git 凭据和 Apple ID 密码。

手动触发、跟进和下载示例（先执行上面的 `$gh` 赋值）：

```powershell
& $gh workflow run ios-unsigned.yml --repo liuxiaogang-com/Citywalk --ref main
& $gh run list --repo liuxiaogang-com/Citywalk --limit 5
# 从上一步结果取得本次运行 ID，再用 run watch <实际ID> --exit-status 跟进。
# 下载一个明确的已发布版本；未来版本用实际标签和新的目标目录。
& $gh release download v0.3.2-build1003 --repo liuxiaogang-com/Citywalk --dir 'D:\Citywalk-downloads\v0.3.2-build1003'
```

其他已使用的本地工具：

- Python：`C:\Users\xiao\AppData\Local\Programs\Python\Python312\python.exe`，读取中文时使用 `-X utf8`。
- Git Bash：`C:\Program Files\Git\bin\bash.exe`。PowerShell 中检查脚本可显式调用：`& 'C:\Program Files\Git\bin\bash.exe' -n scripts/ci-test.sh scripts/ci-build-unsigned.sh`。
- 已下载的 actionlint：`.build/ci-tools/actionlint/actionlint.exe`，仅为本地忽略缓存，新的 Git 克隆不包含它。
- `.gitattributes` 为 Bash 和 YAML 保留 LF 换行，避免 Windows 换行破坏云端脚本。

本次曾遇到 GH CLI 自身凭据缺少 workflow scope，手动创建历史 Release 失败；同账号现有 Git 凭据可完成发布，且内置 GITHUB_TOKEN 的自动发布已验证成功。以后优先使用已配置的自动发布链路；确需手动发布且再次遇到 scope 错误时检查所用身份和权限，不要把 Git 能推送等同于 GH CLI 具有全部相同权限。

## 6. 修改功能时查哪些文件

| 功能 | 主要入口 |
| --- | --- |
| 首页、记录面板、探索 UI、地图渲染 | FogWalk/Views/ContentView.swift、RecordingPanel.swift、ExploreSheet.swift、FogMapView.swift |
| 应用状态及页面/数据协调 | FogWalk/Services/AppModel.swift |
| 定位采集、权限和记录状态 | FogWalk/Services/LocationManager.swift |
| 正常/省电策略与 SQLite 保存 | FogWalk/Models/RecordingPolicy.swift、FogWalk/Services/RecordingStore.swift |
| 导入、备份、缓存与轨迹处理 | FogWalk/Services/TrackDataLoader.swift、TrackDataStore.swift、StartupSnapshot.swift、TrackProcessor.swift |
| 探索、迷雾网格、坐标转换 | FogWalk/Services/ExplorePlanner.swift、ExplorationGrid.swift、ChinaCoordinateTransform.swift |
| 高德/Apple 导航 | FogWalk/Services/ExternalNavigation.swift；NAVIGATION.md |
| 回归测试 | FogWalkTests/ |
| 权限文案、最低系统、签名和版本参数 | FogWalk-Info.plist、FogWalk.xcodeproj/project.pbxproj |

先阅读相关文件再修改；上表是定位入口，不替代当前源码。

## 7. 已知问题与数据边界

已知未修复问题：GPS 从暂时不可用恢复后，状态文字可能仍显示“等待有效 GPS 信号”，但记录已恢复。详细证据和原因在 [SIMULATED_LOCATION_TEST.md](SIMULATED_LOCATION_TEST.md)。原因线索是错误回调设置等待文案，而有效定位后的策略更新可能提前返回。若下一步修复此项，应增加恢复用例，并保留真正写入失败或权限关闭时的提示；本交接未替用户决定下一项改动。

后台/锁屏连续记录、系统挂起后的表现和耗电仍需真机验收。安装步骤见 [WINDOWS_INSTALL.md](WINDOWS_INSTALL.md)。未签名 IPA 需重新签名；覆盖旧版本前先从 App 导出 `.fogwalk` 备份。

`demodata/`、`artifacts/`、`.build/` 和个人 `.fogwalk` 备份等保持 Git 忽略；不要为了让个人数据测试通过而强制上传私人 CSV、GPX 或足迹截图。旧包外层的 README.txt、迁移清单和原始聊天是 2026-09-04 的历史快照，其中“无远程”“尚无 IPA”等说法不能代替本次已验证的新状态。保留迁移资料作为历史依据即可。
