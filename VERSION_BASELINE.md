# V0.1 本地基线

2026-09-04：按用户要求，在 V0.2 修整前保留当前代码版本并本地提交。

- 基线包括原生地图、历史迷雾、CSV/GPX 导入及本地备份、目的地搜索、最多 6 个候选、滑动卡片与地图标记联动。
- 目前实时定位不等于持续足迹存储；后台记录、系统终止恢复和闭环路网尚未完成。
- 基线引用：Git 标签 `v0.1-baseline`。本地仓库不配置远程，不上传。
- 个人原始数据 `demodata/`、截图 `artifacts/`、构建目录 `.build/`、导出档案和签名产物均不入库，原文件保留在磁盘。
- 上轮确认：12 项 XCTest 通过。V0.2 的新测试及状态单独记录在 V02_PLAN.md。

回看基线使用 `git show v0.1-baseline:<文件路径>`；恢复版本前先保留后续修改，不使用破坏性重置。
