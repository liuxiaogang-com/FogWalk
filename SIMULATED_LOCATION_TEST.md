# 实际模拟定位回放验收

日期：2026-09-04；应用版本 0.3.2 / 6，代码基线 `04fa667`。

## 测试目的与隔离

用户询问能否直接出门测试，以及开发环境是否能模拟定位。可以开始外出验收；本轮在独立 iOS Simulator 先验证位置服务到持久化的真实运行链路。

- 独立模拟器：`FogWalk Location QA` / `18311C8C-AAD1-4E6F-B7E4-FF12564F1ADF`，iPhone 17 Pro / iOS 26.5。
- 使用 Release 模拟器构建，未启用 `--ui-fixture`，没有把假轨迹直接导入数据文件或手动调用 delegate。
- `simctl location` 将路线发送到模拟器系统，再由 Core Location 回调进入正式的过滤、SQLite 保存、地图恢复代码。
- 只在独立模拟器授予 Always 位置权限，运动辅助关闭；通过开发启动参数启用记录与选择模式，不操作真实手机权限、不注入手机假位置。
- 真实手机继续保留 0.3.2 和用户数据，本轮无需更新手机 App。

## 测试流程

可复用脚本：`scripts/run-location-journey.sh`，参数为已安装应用的独立模拟器 UUID 和输出目录。

1. 正常模式以 1.5 m/s 模拟步行约 75m，前 20s 留在前台，后段切到系统设置使 App 进入后台。
2. 在终点固定停留 125s，超过正常模式的 120s 静止迟滞阈值；观察是否不断记录同一位置。
3. 保持 App 在后台，再走约 75m，核对记录是否继续增加。
4. 重新启动应用，切换省电模式，以 1.5 m/s 走约 135m。初次跑前台 30s 后切后台，另补前台 45s；脚本后续改为前台 45s 再切后台，以覆盖首次 60m 距离过滤。
5. 清除模拟定位，真正结束 App 进程；不带记录开发参数重新打开，核对 SQLite 点数不丢失，并检查地图中的探索区域。
6. 脚本退出会清除该模拟器的模拟定位；测试模拟器及日志保留用于复查，不删除现有开发模拟器。

## 证据位置

- 构建：`artifacts/simulation-build.log`。
- 阶段汇总：`artifacts/location-journey-run.log`。
- 实际 App 日志：`artifacts/location-journey/normal.stderr.log`、`saver.stderr.log`、`relaunch.stderr.log`。
- 地图恢复截屏：`artifacts/location-journey/relaunch-map.png`。
- SQLite 只读查询按模式和前后台上下文统计；测试中出现的首个冷启动位置可能在界面激活前收到，不能单凭它当作行走期间后台通过。

## 本轮结果

- 主流程脚本退出码 0：正常模式第一段新增 4 点，停留终点补记 1 点后保持不变；125s 后重新移动，后台又新增 4 点，首次新点在约 10s / 15m 后到达。
- 省电模式 135m 行走新增 2 点，位置回调间隔约 40s / 60m，与距离过滤符合；两点均在切后台后接收。该阶段前台仅 30s，未达到 60m，因此不能把启动补点当作省电前台落盘通过；另做前台补测。
- 主流程结束共有 17 个真实走过正式采集链路的模拟位置（含开始前预检 5 点）；清除模拟定位、终止进程再开后仍为 17，地图可恢复足迹参考位置与探索区域。未手工改写数据库。
- 省电前台补测：08:27:48 接收并保存新的前台点。补测后共 19 点（正常前台 6、正常后台 8、省电前台 1、省电后台 4）；再次清除模拟位置和结束进程重开后，只读查询仍为 19 点，唯一键计数也是 19。
- 前台补测截图：`artifacts/location-journey/saver-valid-fix-stale-status.png`，记录 19 点、最新定位 08:27:48，但状态仍旧等待 GPS，进一步确认下面的显示缺陷。
- 最终脚本补强为按阶段增量检查前后台点数，并将省电前台延长到 45s；`bash -n` 通过。本轮运行证据来自初始主流程 + 单独的前台补测，没有将改进脚本宣称为再次完整回放通过。
- 稀疏点数不等于耗电实测；未证明硬件省电或系统真正挂起后唤醒。

### 发现的问题：GPS 恢复后状态提示可能滞留

- 08:25:04 回放切换时 Core Location 暂报 `kCLErrorDomain code=0`（locationUnknown），随后同秒已收到精度 5m、age=0 的有效位置。
- `saver-foreground.png` 显示最近定位时间更新、活动为“移动中”，但状态仍为“等待有效 GPS 信号”。后续 08:25:44 与 08:26:24 新点仍正常写入。
- 原因定位：LocationManager.didFailWithError 设置等待文案；didUpdateLocations 中记录分支靠 applyPolicy 更新文案，但配置和静止状态未变时 applyPolicy 会提前返回，因此恢复信号不会自动清掉旧文案。
- 影响：显示误导，不是本次回放中的记录中断。此处尚未修正；本轮是测试和诊断，没有修改生产逻辑或升级真实手机。
- 后续修正应在有效位置恢复时刷新状态，且不得覆盖真正的写入失败/权限关闭提示；需要补故障恢复用例。

测试结束已清除模拟定位并关闭本轮新建的 QA 模拟器，保留测试数据库和产物供复查。真实手机未改变。

## 不能用模拟结果证明的内容

- 模拟器的位置精度固定、运动数据理想，不代表真实 GPS 漂移、隧道和室内丢星。
- 模拟器切后台不等于实体手机真实锁屏、挂起、系统回收或区域唤醒。
- 停留期间不写重复点不等于 GPS 硬件已经省电；是否进入低功耗、运动权限能省多少电，必须真机测量。
- 本轮不注入 Core Motion 运动识别，因此不证明活动识别准确率。
- 本轮没有测试高德实际导航过程，也不以脚本启动记录替代开始/结束记录按钮的交互验收。

## 外出建议

先正常模式，精确位置开启，位置权限建议“始终”，点开始记录后确认最近定位时间更新。
前台走 2 分钟、切其他 App 走 5 分钟、锁屏走 10 分钟，中途停 2–3 分钟再走，不划掉 App。
回来结束记录、查看今日轨迹，并导出备份。再用省电模式做相近路线与条件的对照。

官方依据：[Apple：模拟定位测试](https://developer.apple.com/documentation/xcode/simulating-location-in-tests)。
