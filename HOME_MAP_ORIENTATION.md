# V0.3.3 首页朝向与实时定位

日期：2026-09-13。

## 操作

- 首页底部为“图层 / 北方朝上（或手机朝向）/ 定位”。朝向按钮打开菜单，选择会被本地记忆。
- 新启动 App 后，首页取得有效位置会按记住的朝向自动跟随；手动浏览后可用定位按钮恢复。
- 北方朝上：地图上方保持北方，当前位置箭头随手机转向。
- 手机朝向：取得新定位后持续以当前位置居中，地图随手机方向旋转，箭头指向屏幕上方。
- 定位：显示“定位中”，等待点击之后产生的有效 GPS 定位，再恢复当前位置居中、局部缩放和所选朝向。不会用点击前的缓存位置完成本次定位。
- 拖动或缩放地图后暂停跟随，方便查看其他区域；再次点击定位可恢复。查看足迹全览也会暂停跟随。
- GPS 超时或权限关闭时明确提示；没有可靠方向时显示位置圆点，不伪造方向箭头。手机保持竖屏，以手机顶部方向作为朝向。

## 实现边界

`HomeMapLocation` 单独管理首页前台 GPS 与罗盘，不更改 `LocationManager` 的正常/省电记录策略，也不向轨迹数据库写入浏览位置。退出首页进入探索页或 App 进入后台时停止该组传感器；定位授权弹窗导致的短暂 inactive 不取消请求。

GPS 只接受精度不超过 100 米、30 秒内、坐标有效且时间未倒退的位置。本次定位必须满足位置时间戳不早于点击时间；20 秒内没有有效新定位则提示重试。地图坐标沿用现有中国大陆显示转换，原始档案不变。

罗盘优先使用真北方向，暂不可用时使用磁北方向；负精度及无效数值不参与旋转。方向来自手机罗盘，并非 GPS 行进方向。磁性车架、室内环境及设备传感器可能影响实际表现。

`FogMapView` 的首页模式显示独立位置箭头，并在更新朝向/位置时复用迷雾与轨迹覆盖层。探索页的推荐、选点、路线展示保留现有方式；共用的定位镜头重置补充了 heading 和 pitch 归零。

这次提供前台地图居中与朝向跟随；没有增加逐转向语音导航、道路吸附或车道引导。保持首页开启会使用前台 GPS 和罗盘，实际耗电需真机测试。

## 回归与验收

最终源码 `4634eec899ffabaa00b3d6224f8ad056bfe3da86` 经 [Actions 34743248166](https://github.com/liuxiaogang-com/Citywalk/actions/runs/34743248166) 验证：46 项通过、1 项按既有逻辑因模拟器定位权限跳过、0 失败，另有 3 项个人数据测试预先排除。已发布 [v0.3.3-build1006](https://github.com/liuxiaogang-com/Citywalk/releases/tag/v0.3.3-build1006)，Windows 下载后通过 SHA-256、ZIP CRC、arm64/iPhoneOS、未签名和可执行权限校验。真机罗盘、行走跟随和耗电尚待安装后验收。

- `HomeMapLocationTests`：旧缓存不能完成新请求、重复定位、无效/过期/乱序定位、后台停止传感器、拒绝权限、手势取消、朝向偏好与无效方向。
- `MapInteractionTests`：旋转后再次定位回正、手机朝向与位置箭头、移动跟随、手势暂停、切回北方、定位过期移除标记、更新时迷雾覆盖层复用。
- Windows 只执行适用静态检查；编译与 XCTest 结果以本次 GitHub Actions 日志为准。
- 真机安装后：在开阔处点击定位，核对当前位置；选择手机朝向并原地转身、短距离行走；切回北方，确认地图北方朝上而箭头继续转动；拖动地图后再次定位；切后台再返回。另检查定位权限关闭、精准定位关闭、室内无 GPS 的提示。

技术依据：[Apple 罗盘与行进方向说明](https://developer.apple.com/documentation/corelocation/getting-heading-and-course-information)、[MKMapCamera](https://developer.apple.com/documentation/mapkit/mkmapcamera)。本次 UI 使用 SF Symbols 与按压/状态反馈，对应 buerguo 规则 ui-003、ui-yikgd9、ui-016。
