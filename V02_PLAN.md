# V0.2 第一轮：界面统一与探索体验

后续进展（2026-09-04）：V0.2 已成功覆盖安装到手机；V0.2.1 进一步完成启动性能优化并覆盖安装，最新证据见 [STARTUP_PERFORMANCE.md](STARTUP_PERFORMANCE.md)。下方保留本轮原始验收记录。

## 用户确认与范围

2026-09-04 用户同意先保存现有版本、本地提交，再把新版本修整作为任务目标执行，并将上下文写入本地。
“监听”暂按“版本快照”理解，已向用户说明并提供纠正入口。

本轮原则：地图主体、探索主线、辅助功能按需出现；不新增底部多 Tab。
本轮不包含：持续轨迹写入、锁屏后台记录、电量专项、探索结束报告、完整历史日历、完整路网、闭环或自研逐向导航。这些属于后续阶段，不能将原有记录按钮描述为已完成的持续记录。

## 实施与验收清单

- [x] 建立本地 Git 基线提交和 v0.1-baseline 标签；排除个人数据和签名产物。提交 `039c12b`。
- [x] 首页：轻量摘要、更多菜单、定位与图层工具；日期回顾弱化；累计迷雾不随日期缩小；空库可在定位后直接探索。
- [x] 探索：紧凑的顶部条件、明确单程时间含义；保存条件；候选卡片两行名称、简短地址和真实指标；卡片/地图选点双向联动；减少自动飞图，提供主动路线总览。
- [x] 搜索：新条件作废旧请求；支持取消/重试；“换一批”抑制重复且不过度偏向品牌；候选仍硬性排除已探索终点。
- [x] 时间与指标：预算内结果使用路线 ETA 筛选；没有路线时只能显示估算时间、直线距离、路线待确认，不能显示未知 0% 冒充计算结果。
- [x] 手动选点：保留显式入口；反查地址后可先在 App 内预览路线，再自愿打开 Apple 地图；取消/重选不会串结果。
- [x] 权限、无结果、网络失败和空库有清楚状态和操作入口。
- [x] 本地构建与单元回归；20 项测试通过，隔离合成数据的六种界面截图已检查。
- [x] 检查设备连接：iPhone 16 Pro 显示 unavailable，本轮未安装；连接恢复后可覆盖安装，不卸载、不清除用户数据。

## 必须保留的规则

- 已探索核心半径 50 米，连接必须符合时间、距离和精度约束；原始坐标保存与地图显示坐标分离。
- 默认历史轨迹关闭、迷雾开启；首页定位回到实时位置与约 3 公里缩放。
- 地图默认局部视野；无实时定位时明确标注历史位置来源，不伪称当前位置。
- 手动选点允许用户选择去过的位置，自动推荐不得推荐已探索终点。
- 个人数据仅在本地；不做外部上传，不新增远程 Git。

## 进度记录

- 开始时当前目录没有 Git 仓库，现已建立本地基线和标签。
- 已完成 V0.2 第一轮实现、本地测试及截图检查，版本号 0.2，构建号 2。
- 完整三轮产品建议见本任务对话；已有技术规格见 PRODUCT_SPEC.md。本文件是本轮执行和接续入口。

## 实现说明

- 首页“记录”收进“前台定位测试”，明确告知尚未持续保存。不能让按钮状态冒充完整记录能力。
- 条件用 UserDefaults 保存，不保存定位信息；目的地选择保存在本次 App 会话中。
- 用户滑动卡片时仅更新候选路线与标记，不重建历史迷雾；首次展示可自动总览，用户移动过地图后需主动点击“总览”。
- 从远距离/全国视野点击定位时直接跳回局部；同城短距离使用动画。
- 没有严格保证每次 6 个结果；数据有限时可少于 6 个，候选耗尽时明确提示，不能放回已探索点凑数。
- 地图标记/路线检查与视觉样例均为合成测试数据，不代表真实步行可通行路线。

## 后续阶段（未实施）

第二轮：采集新位置并增量落盘、实时揭雾、重启恢复、探索进行中和结束摘要、历史日期详情，再专项实测锁屏、权限、耗电与系统终止恢复。
第三轮：导入/备份细节与状态、地图偏好、局部缓存和完整路网探索；闭环必须有真实多段道路路线，不以最远目的地点代替环线。

## 验证记录（持续更新）

- 首轮 18 项数据/规则测试通过。
- 新增 2 项 SwiftUI + MKMapView 集成测试；初次测试宿主未挂入 UIWindowScene，修复测试宿主后卡片/标记/路线联动通过。
- 连续定位检查暴露从远处返回的长动画等待问题，改为远距离直接恢复局部视野；最终回归 20 项通过、0 失败、0 跳过。包含日期/轨迹等原有规则、搜索条件持久化与失效、类别与批次筛选、真实指标、地图点选联动、保留迷雾图层和重复定位。
- 测试结果：`/Users/xiao/Library/Developer/Xcode/DerivedData/FogWalk-gheyousbgdfsehgmwultxvdoufrs/Logs/Test/Test-FogWalk-2026.09.04_07-28-40-+0800.xcresult`。
- 最终签名构建通过：`xcodebuild -quiet -project FogWalk.xcodeproj -scheme FogWalk -configuration Debug -destination 'generic/platform=iOS' -derivedDataPath .build/DeviceDerivedData -allowProvisioningUpdates build`。包内版本确认 0.2 (2)，未包含 CSV、GPX、fogwalk 个人备份文件。
- 模拟器 iPhone 17 Pro / iOS 26.5 截图检查：首页、探索结果、目的地详情、路线未确认、手动路线预览、无结果错误状态。截图在 `artifacts/v02-*.png`，不提交 Git。未发现当前默认字号下的主要控件重叠；详情可滚动。
- 截图使用 `DEBUG && targetEnvironment(simulator)` 隔离样例，启动参数 `--ui-fixture`，不读取或写入用户存档；示例地点和路线不是实际搜索结果，不用于证明真实路线可达。
- 收尾设备检查仍为 iPhone unavailable；本轮未覆盖安装、未卸载、未触碰真机数据。

## 接续说明与未验证项

- 本轮目标已在本地完成；手机连接后优先覆盖安装 `.build/DeviceDerivedData/Build/Products/Debug-iphoneos/FogWalk.app`，Bundle ID 保持 `com.citywalk.FogWalkDemo`。
- 待真机验证：用户实际位置附近的咖啡/茶饮/餐饮召回、真实路线服务可用性、实际触摸滑动与地图选点、定位权限及断网恢复。自动测试和合成截图不能替代上述验证。
- 尚未专项验证：小屏、极大辅助字号、VoiceOver、耗电、后台持续记录。后两项不属于本轮已实现功能。
- 本地基线是 `039c12b` / `v0.1-baseline`；V0.2 使用独立提交，可用 `git log --oneline -2` 查看。未配置远程，也未上传个人数据。
