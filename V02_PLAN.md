# V0.2 第一轮：界面统一与探索体验

## 用户确认与范围

2026-09-04 用户同意先保存现有版本、本地提交，再把新版本修整作为任务目标执行，并将上下文写入本地。
“监听”暂按“版本快照”理解，已向用户说明并提供纠正入口。

本轮原则：地图主体、探索主线、辅助功能按需出现；不新增底部多 Tab。
本轮不包含：持续轨迹写入、锁屏后台记录、电量专项、探索结束报告、完整历史日历、完整路网、闭环或自研逐向导航。这些属于后续阶段，不能将原有记录按钮描述为已完成的持续记录。

## 实施与验收清单

- [ ] 建立本地 Git 基线提交和 v0.1-baseline 标签；排除个人数据和签名产物。
- [ ] 首页：轻量摘要、更多菜单、定位与图层工具；日期回顾弱化；累计迷雾不随日期缩小；空库可在定位后直接探索。
- [ ] 探索：紧凑的顶部条件、明确单程时间含义；保存条件；候选卡片两行名称、简短地址和真实指标；卡片/地图选点双向联动；减少自动飞图，提供主动路线总览。
- [ ] 搜索：新条件作废旧请求；支持取消/重试；“换一批”抑制重复且不过度偏向品牌；候选仍硬性排除已探索终点。
- [ ] 时间与指标：预算内结果使用路线 ETA 筛选；没有路线时只能显示估算时间、直线距离、路线待确认，不能显示未知 0% 冒充计算结果。
- [ ] 手动选点：保留显式入口；反查地址后可先在 App 内预览路线，再自愿打开 Apple 地图；取消/重选不会串结果。
- [ ] 权限、无结果、网络失败和空库有清楚状态和操作入口。
- [ ] 本地构建与单元回归；使用隔离测试数据验证界面，明确区分自动测试、人工/截图检查与未验证项。
- [ ] 当前设备可用时覆盖安装，不卸载、不清除用户数据；无法连接则如实记录。

## 必须保留的规则

- 已探索核心半径 50 米，连接必须符合时间、距离和精度约束；原始坐标保存与地图显示坐标分离。
- 默认历史轨迹关闭、迷雾开启；首页定位回到实时位置与约 3 公里缩放。
- 地图默认局部视野；无实时定位时明确标注历史位置来源，不伪称当前位置。
- 手动选点允许用户选择去过的位置，自动推荐不得推荐已探索终点。
- 个人数据仅在本地；不做外部上传，不新增远程 Git。

## 进度记录

- 已检查当前目录：尚未建立 Git 仓库。
- 已阅读当前首页、探索页、定位与搜索实现；开始建立基线。
- 完整三轮产品建议见本任务对话；已有技术规格见 PRODUCT_SPEC.md。本文件是本轮执行和接续入口。
