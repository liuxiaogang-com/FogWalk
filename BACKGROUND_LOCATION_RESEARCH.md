# 后台定位续跑与省电方案

更新时间：2026-09-13。

## 结论

迷雾足迹不应依赖“每隔 N 分钟运行一次”的后台定时器。iOS 会在应用进入后台后挂起普通任务，可靠方案是让 Core Location 管理一次由用户明确开启的长期记录会话：移动时交付标准定位，静止时自动暂停或降级，由显著位置变化、区域退出和到访事件低功耗唤醒。系统若清理进程，定位事件可重新启动未被用户强制划掉的 App，App 必须在启动阶段立即恢复此前的记录会话。

“一生足迹”的公开说明与此一致：要求始终定位、精确位置、后台 App 刷新且不要划掉 App；其省电模式也明确说明静止时不后台运行，可能降低精度或漏记。因此目标不是让 Swift 代码永久常驻，而是让系统保留并恢复定位任务。

## 本项目此前的缺口

- 已有 `UIBackgroundModes/location`、`allowsBackgroundLocationUpdates`、标准定位、显著位置变化、静止区域退出唤醒和启动恢复。
- 真机历史日志只有“使用 App 期间”授权。在该权限下，短时间切后台可继续，但系统挂起或清理进程后的恢复能力不足。
- 省电策略会降精度、加大距离过滤，并在“始终允许”时于静止阶段停止标准定位；这一方向正确，不应改成后台定时轮询。
- 缺少 iOS 17+ 的 `CLBackgroundActivitySession`。它本身不请求 GPS，只维持一次对用户可见的后台定位会话，使后台事件交付和恢复更明确。
- UI 未显示“后台 App 刷新”是否关闭，用户难以判断为什么显著位置/区域事件没有重新唤醒 App。

## 本轮实现

- 用户首次点击“开始记录”后继续持久化记录意图；重新打开或由定位事件在后台拉起时自动恢复。
- 记录期间持有 `CLBackgroundActivitySession`，结束记录时显式失效，不用虚假音频或无限后台任务保活。
- “始终允许”时同时注册显著位置变化和到访监测；系统暂停标准定位或省电静止后，继续依靠低功耗位置事件恢复。
- 记录面板显示后台 App 刷新和后台续跑就绪状态，并明确首次设置和系统边界。
- 保留既有正常/省电采样阈值、运动辅助开关、SQLite 增量保存和个人数据格式。

## 必须满足的用户设置

1. 首次打开记录面板，点击“开始记录”。
2. 位置权限选择“始终”，并开启“精确位置”。如果 iOS 先只给“使用 App 期间”，在记录面板再次申请或进入系统设置升级。
3. 系统设置中开启后台 App 刷新。
4. 不从多任务界面划掉 App；划掉代表用户明确终止，iOS 不保证再由定位事件拉起。

## 预期精度与耗电

- 正常模式用于步行轨迹细节，运动时请求最佳精度并使用较小距离过滤，耗电更高。
- 省电模式降低系统期望精度和打点频率；静止后停止标准定位，以显著位置、区域和到访事件等待恢复。刚起步的短路段可能漏记，系统事件也不是固定时刻到达。
- 显著位置变化通常是数百米级、分钟级的保险通道，不能替代跑步级连续轨迹。它的价值是低功耗唤醒和进程恢复。
- App 只能请求所需精度与活动类型，最终采样、暂停、重启时机由 iOS 决定，不应承诺“永不丢点”或固定间隔。

## 真机验收

1. 确认记录面板显示“始终允许”“精确位置已开启”“后台 App 刷新已开启”“后台续跑已就绪”。
2. 正常模式：前台步行 2 分钟、切其他 App 10 分钟、锁屏 20 分钟；检查点数、时间连续性和系统电池统计。
3. 静止至少 10 分钟，再步行 1 公里；验证暂停后能恢复，并记录恢复前可能遗漏的距离和时间。
4. 让 App 留在后台过夜，不划掉；第二天步行 1 公里，确认能由系统事件续跑。
5. 分别关闭后台 App 刷新、降为“使用 App 期间”、手动划掉 App，验证 UI 能指出限制，且不把这些系统边界误报为已通过。
6. 正常与省电走同一路线，对比系统“设置 > 电池”中的耗电和轨迹缺口。模拟器测试不能代替以上系统挂起、进程清理和耗电验收。

## 技术依据

- [Apple：在 SwiftUI 中支持后台实时定位与生命周期恢复](https://developer.apple.com/documentation/corelocation/supporting-live-updates-in-swiftui-and-mac-catalyst-apps)
- [Apple：后台定位处理](https://developer.apple.com/documentation/corelocation/handling-location-updates-in-the-background)
- [Apple：显著位置变化监测](https://developer.apple.com/documentation/corelocation/cllocationmanager/startmonitoringsignificantlocationchanges())
- [Apple：定位节能建议](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/EnergyGuide-iOS/LocationBestPractices.html)
- [一生足迹官方使用说明](https://doc.steplife.cn/md/guide.html)
- [一生足迹官方常见问题](https://doc.steplife.cn/md/faq.html)

## V0.3.5 整合复核

保留后台定位会话、显著位置变化、到访监测和定位事件启动恢复。补充结束记录后忽略迟到的标准定位恢复回调，打开记录面板时刷新系统权限状态。新增回归覆盖：没有记录意图时定位事件不能自动开录；明确结束记录后恢复/区域回调不能改变停止状态或重新开启记录。

Windows 已完成代码复核、权限声明和静态检查；Xcode 编译与完整测试由本次 Actions 验证。模拟器不会证明系统清理后、后台过夜或锁屏后的真实恢复能力，以上真机验收仍必需。
